document.addEventListener('DOMContentLoaded', function () {
    var insta = document.getElementById('insta');
    insta.addEventListener('animationend', function () {
        insta.classList.add('installed');
    });
});
document.addEventListener('DOMContentLoaded', function () {
    var inst = document.getElementById('instasss');
    inst.addEventListener('animationend', function () {
        instasss.classList.add('installedss');
    });
});